import {
    GET_POSTS,
    ADD_POST,
    GET_ERRORS,
    DELETE_POST,
    GET_POST

} from '../actions/types';

const postReducerDefaultState = [];

export default (state = postReducerDefaultState, action) => {
    switch(action.type) {
        
    }
}
